
version: '3.8'
services:
  db:
    image: postgres:15-alpine
    container_name: seo_postgres_dev
    restart: unless-stopped
    environment:
      POSTGRES_DB: seo_platform_dev
      POSTGRES_USER: seo_dev_user
      POSTGRES_PASSWORD: DevStrongP@ss123!
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
